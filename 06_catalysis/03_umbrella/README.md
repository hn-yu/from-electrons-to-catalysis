# 03 · 从有偏轨迹恢复自由能

本项目通过多个带偏置的采样窗口探索能垒区域，再去除已知偏置恢复平衡自由能，理解采样覆盖和重加权各自解决什么问题。

## 背景：为什么需要这一步

高能区域的 Boltzmann 概率很小，普通采样可能几乎从不访问它。由未跨越能垒的短轨迹直接画直方图，会把“没采到”误认为“概率为零”。伞形采样用人为势把体系约束在不同窗口附近，让原本罕见的区域获得数据。

偏置改变了所采分布，因此不能把各窗口的直方图直接相加当作原体系平衡概率。WHAM（加权直方图分析）自洽估计各窗口归一化，再组合成无偏概率；MBAR（多态 Bennett 接受率方法）用样本在多个状态下的约化能量进行重加权。本项目用 PyMBAR 作为独立实现。

重加权只能处理已经采到的信息。即使一维窗口重叠良好，未被偏置的隐藏坐标仍可能困在不同盆地。为此这里使用可解析积分的二维势，并另设隐藏慢变量反例，让“算法一致”和“采样充分”的区别可直接观察。

## 开始前需要理解的概念

- **集体变量 CV**：用于描述进程的低维坐标，本例为 x；未显式画出的 y 仍影响自由能。
- **窗口偏置 Wk**：已知的附加谐势，用于集中采样某一区域。
- **窗口重叠**：相邻状态共同采到的区域，用来确定相对归一化。
- **约化势 u=βU**：以 kBT 为能量单位的无量纲势，供重加权使用。
- **自相关与混合**：相邻样本可能高度相关，访问多个盆地的能力另需检查。
- **Metropolis 步**：生成平衡分布的随机采样更新，不对应这里的真实物理时间。

## 本次任务：从什么得到什么

从 windows.csv 读取 13 个中心与弹簧常数，用手写 Metropolis 生成并保存各窗口轨迹，丢弃初始平衡段。自己实现分箱 WHAM，再把同一批实际样本交给 PyMBAR FES；与解析边缘自由能比较，同时检查接受率、窗口重叠和隐藏坐标行为。

## 先用一个小例子走通思路

模型 $U(x,y)=0.15(x^2-1)^2+(1+x^2)y^2$ 在 x=0 附近的 y 分布比 x=±1 更宽。把 y 积分掉后，会多出 $\tfrac12 k_BT\ln(1+x^2)$，所以恢复的自由能不只是 y=0 上的势能。

若某个窗口从未访问某一 y 盆地，WHAM 和 PyMBAR 仍可能给出很接近的曲线，因为它们都在处理同一份缺失信息的数据。换初态、独立随机种子和更好的变量，是对此最直接的检查。

## 实现边界

**手写后比对** Metropolis 与直方图 WHAM；对同一批轨迹用 **PyMBAR FES** 独立重加权。实际原子轨迹应由成熟 MD/偏置软件产生，本例解析二维势用来知道正确边缘自由能。

## 输入与物理模型

- [control.toml](input/control.toml)
- [windows.csv](input/windows.csv)

`windows.csv` 每行给出中心 $c_k$ 和弹簧 $K_k$，模型坐标无量纲，K 的单位为 eV。窗口偏置 $W_k(x)=K_k(x-c_k)^2/2$。

```math
U(x,y)=0.15(x^2-1)^2+(1+x^2)y^2.
```

积分掉 y 得到（差一个任意常数）

```math
F(x)=0.15(x^2-1)^2+\frac{k_BT}{2}\ln(1+x^2)+C.
```

WHAM 的分箱概率自洽方程为

```math
p_b=\frac{\sum_kn_{kb}}{\sum_kN_k\exp[\beta(f_k-W_{kb})]},\quad e^{-\beta f_k}=\sum_bp_be^{-\beta W_{kb}}.
```

零计数箱没有自由能信息，不能用零填充。PyMBAR 使用各真实样本位置处的偏置，WHAM 使用箱中心偏置，因此两者在有限箱宽下不要求逐位相等。

## 从公式到程序

1. 每个窗口做有偏 Metropolis，丢弃 burn 步后保存 window-XX.csv 轨迹。
2. 对同一组 bins 统计 counts，用对数域 WHAM 求 p 与 F。
3. 构造每个样本在全部窗口下的 reduced potential u_kn，交给 PyMBAR FES。
4. 保存 F-comparison.csv、窗口接受率和隐藏坐标均值；只在采样充分区域对齐常数后比较曲线形状。

从仓库根目录运行，计算集群上提交 Slurm：

```bash
sbatch scripts/slurm.sh 06_catalysis/03_umbrella/run.py --output runs/03_umbrella
```

个人电脑可在已安装环境中用 `python` 替代 `sbatch scripts/slurm.sh`。`--input` 接收输入目录。先看 [output/report.txt](output/report.txt)，再检查 [output/README.md](output/README.md) 所列中间量；JSON 只保留可追溯记录，读懂结果不需要解析它。

## 应当做的实验

- 当前同样本 WHAM/PyMBAR 的形状 RMS 差约为 0.00005 eV（一维）与 0.00015 eV（二维）；缩小箱宽会改变离散偏差和统计噪声。
- 将相邻窗口间距加大，观察重叠消失；求解器停止迭代并不能保证所有自由能盆地有可靠连接。
- bad_coordinate 示例从 y=−1 和 +1 出发，x 可以重叠但 y 不跨越。解释为什么漂亮的一维直方图无法证明整体平衡。
- 增加独立随机种子、分析相关时间，再讨论误差条；当前示例不把每个相邻轨迹点当独立样本。

## 分步提示与资料

[PyMBAR FES 官方接口](https://pymbar.readthedocs.io/en/4.0.2/fes_with_pymbar.html)。

## 完成后应能解释什么

先看 window-XX.csv 和重叠/隐藏变量诊断，再看 F-comparison.csv。只在采样充分区对齐自由能常数后比较形状。完成后应能区分窗口连接不足、分箱偏差和隐藏变量未平衡，且不把相邻采样点当独立样本来夸大精度。

## 与前后项目的关系

需要 [配分函数](../../04_thermodynamics/01_partition/README.md) 的概率观点；下一项 [MEP 与 FES](../04_mep_fes/README.md) 解析推导同一模型为何具有熵修正。

[输入文件说明](input/README.md) · [输出阅读指南](output/README.md) · [分步提示](hints/README.md) · [全课程实现边界](../../docs/IMPLEMENTATION_BOUNDARIES.md)
