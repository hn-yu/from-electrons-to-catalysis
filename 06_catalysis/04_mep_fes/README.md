# 04 · 最低能量路径为什么不是自由能路径

本项目对同一个二维势分别做最小化和统计积分，直接展示最低能量路径与有限温自由能剖面为何不同。

## 背景：为什么需要这一步

将高维体系投影到一个反应坐标 x 时，可以问两个不同问题：固定 x 后最低能构型有多高，或者固定 x 后所有可访问构型的总统计权重有多大。前者是最小化，后者是积分，通常不产生相同函数。

某个位置虽然最低能量稍高，却可能允许更宽的横向涨落，因而有更多微观态贡献概率。这个构型数量差形成熵项。自由能 F(x)=−kBT ln p(x)+C 因此同时反映最低势能和该位置周围可访问的空间。

本模型的横向势是 Gaussian 积分可解的谐形式，能够在没有采样噪声的情况下隔离这种区别。这里得到的是指定坐标和积分测度下的一维边缘自由能，不自动确定任意实际反应的最佳动力学坐标。

## 开始前需要理解的概念

- **MEP 能量**：本例在固定 x 下对 y 最小化，得到 y=0 上的 V(x)。
- **边缘概率**：对未保留坐标积分后的概率密度。
- **FES**：自由能面或剖面，依赖所选坐标及统计条件。
- **横向曲率**：决定允许涨落的宽度，越硬则可访问宽度越小。
- **自由能常数 C(T)**：同温度相对比较可消去，不应用分别平移后的曲线比较绝对跨温度自由能。

## 本次任务：从什么得到什么

读取温度表，对每个温度计算最低势能 V(x)、自己推导的 Gaussian 积分 F(x)，并用 SciPy quad 独立数值积分 y 核对。分别寻找各温度的自由能最低点，再比较势垒；不要固定使用零温最低点作为所有温度参考。

## 先用一个小例子走通思路

在 x=0 与 x=1，横向系数分别为 1 与 2。积分宽度之比为 $\sqrt2$，因此相对于 x=1，中央区域得到 $\tfrac12k_BT\ln2$ 的熵优势。

600 K 时这一项约 0.0179 eV，已能改变势垒。更精确的自由能势垒还要重新寻找 F 的极小值；它一般会偏离势能的 x=±1。

## 实现边界

**手写**横向高斯积分与熵项；**用 SciPy quad 独立积分比对**。这个解析模型服务于理解 NEB 与采样的区别，真实自由能不应由最低能量路径直接冒充。

## 输入与物理模型

- [control.toml](input/control.toml)
- [temperatures.csv](input/temperatures.csv)

模型与上一项目一致：$U(x,y)=V(x)+(1+x^2)y^2$，$V(x)=0.15(x^2-1)^2$。

沿 y 最小化给出 MEP：$y=0,E_{MEP}(x)=V(x)$。而边缘自由能是积分：

```math
e^{-\beta F(x)}\propto\int_{-\infty}^{\infty}e^{-\beta U(x,y)}dy=e^{-\beta V(x)}\sqrt{\frac{\pi k_BT}{1+x^2}},
```

```math
F(x)=V(x)+\frac{k_BT}{2}\ln(1+x^2)+C(T).
```

$C(T)$ 与 x 无关，可在同一温度比较势垒时消去。不同温度下不能任意比较被分别平移后的绝对自由能。

## 从公式到程序

1. 读温度表，计算 V(x) 和解析 F(x)。
2. 对每个 x 用 SciPy quad 对 y 从负无穷到正无穷积分，独立恢复 F。
3. 每个温度分别减去实际最低 F；不要把 0 K 的 x=±1 固定当成有限温最低点。
4. 输出每温度的 MEP、解析 F、积分 F 与最大误差。

从仓库根目录运行，计算集群上提交 Slurm：

```bash
sbatch scripts/slurm.sh 06_catalysis/04_mep_fes/run.py --output runs/04_mep_fes
```

个人电脑可在已安装环境中用 `python` 替代 `sbatch scripts/slurm.sh`。`--input` 接收输入目录。先看 [output/report.txt](output/report.txt)，再检查 [output/README.md](output/README.md) 所列中间量；JSON 只保留可追溯记录，读懂结果不需要解析它。

## 应当做的实验

- 提高温度，预测中央区域更宽的横向分布如何降低自由能势垒。
- 将横向曲率改为常数，F 与 MEP 应只相差一个与 x 无关的常数。
- 通过求导推算自由能极小值的移动，再与离散网格最小值比较。
- 解释 NEB 收敛和 FES 采样收敛各自回答了哪一个数学问题。

## 分步提示与资料

[SciPy quad](https://docs.scipy.org/doc/scipy/reference/generated/scipy.integrate.quad.html)。

## 完成后应能解释什么

把 MEP、解析自由能和数值积分曲线放在同温度同零点下阅读。完成后应能从横向积分推导熵项，并说明 NEB 力收敛与自由能采样收敛为何不能互相替代。

## 与前后项目的关系

与 [伞形采样](../03_umbrella/README.md) 使用同一可解势；回看 [NEB](../../05_kinetics/04_neb/README.md) 和 [TST](../../05_kinetics/05_tst/README.md)，明确势垒的物理定义后再进入 [反应网络](../05_reaction_network/README.md)。

[输入文件说明](input/README.md) · [输出阅读指南](output/README.md) · [分步提示](hints/README.md) · [全课程实现边界](../../docs/IMPLEMENTATION_BOUNDARIES.md)
