# Reference output

`result.json` is generated from the adjacent input by the reference implementation. It records the input hash, dependency versions and scheduler job. `analysis.md` interprets the measured result and records prediction failures. Additional CSV and structure files are generated where useful.
