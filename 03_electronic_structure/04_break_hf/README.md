# 04 · 让 Hartree–Fock 暴露自己的近似

本项目使用已经成熟的 RHF/UHF 求解器，分别改变键长、自旋约束和基组，学习判断一个数值收敛的电子结构结果在物理上是否可信。

## 背景：为什么需要这一步

上一项验证了自己的 SCF 程序，但程序正确只保证它求解了指定近似。化学反应会改变成键方式；一个在平衡结构附近合理的近似，沿着断键路径可能逐渐失效。因此电子结构学习还需要主动设计能暴露近似的实验。

RHF 将两个反自旋电子放进同一个空间轨道。H₂ 被拉得很远时，物理图像应趋向两个各有一个电子的氢原子，单一双占据轨道却不能正确表达全部相关性。UHF 允许 α、β 电子使用不同空间轨道，可以通过破缺对称降低能量，但所得行列式可能混入不同总自旋成分。

另一类误差来自用于展开轨道的基函数不够。增大基组是在扩大表示空间，改变 RHF 为 UHF 是在改变波函数约束；这两种操作解决的问题不同。本项目把它们拆成独立实验，避免看到能量降低就直接宣称模型已正确。

## 开始前需要理解的概念

- **电子相关**：电子的联合运动不能完全由单行列式平均场描述；交换已包含在 HF 中，不能将两者混为一谈。
- **自旋 S 与多重度**：纯态的 S² 期望值为 S(S+1)，多重度为 2S+1。
- **PySCF spin 参数**：指定 Nα−Nβ；本案例以它设置目标自旋投影，spin=2 对应三重态计算的常用设置。
- **破缺对称初猜**：给两种自旋不同的初始空间分布，帮助找到对称初猜可能遗漏的解。
- **自旋污染**：UHF 行列式未必是总自旋算符的本征态；能量与 S² 需要一起看。

## 本次任务：从什么得到什么

从 geometries.csv 指向的五个 H₂ XYZ 构建键长扫描，用 PySCF 独立求 RHF 和 UHF；另外在固定 O₂.xyz 上比较自旋设置，在固定 H₂ 几何上比较基组。自己负责控制变量、组织能量/S²/收敛表，并解释三个实验各检验什么。

## 先用一个小例子走通思路

纯单重态 H₂ 的 $S=0$，所以应有 $\langle S^2\rangle=0$。远距离破缺对称 UHF 若得到接近 1，虽然能量比 RHF 低，却没有得到一个纯单重态波函数。

如果只是把收敛阈值从较松改得更紧，S² 仍然接近 1，这不是提高迭代精度就能消除的误差。反过来，使用对称初猜可能让 UHF 留在 RHF 型解；“允许破缺”并不等于“已经找到破缺解”。

## 实现边界

本项目**直接用 PySCF RHF/UHF**。上一项目已经手写过 RHF，此处手写的是受控实验和物理诊断：能量差、自旋污染、基组与方法误差的拆分。不要再写一个不完整 UHF 当作成熟参考。

## 输入与物理模型

- [H2_0.74.xyz](input/H2_0.74.xyz)
- [H2_1.50.xyz](input/H2_1.50.xyz)
- [H2_2.00.xyz](input/H2_2.00.xyz)
- [H2_3.00.xyz](input/H2_3.00.xyz)
- [H2_5.00.xyz](input/H2_5.00.xyz)
- [O2.xyz](input/O2.xyz)
- [control.toml](input/control.toml)
- [geometries.csv](input/geometries.csv)

`geometries.csv` 指向不同键长的 H₂ XYZ，键长重新从坐标读取；`O2.xyz` 是另一组自旋实验，不能混入 H₂ 拉伸曲线。

RHF 强制 $\phi_i^\alpha=\phi_i^\beta$；UHF 允许两套空间轨道独立变化。因此同一基组和电子数下，UHF 的变分空间包含 RHF。

```math
E_{\rm UHF}\le E_{\rm RHF}\quad\text{（若找到相应最低驻点）}.
```

纯自旋态应满足 $\langle\hat S^2\rangle=S(S+1)$。解离极限的破缺对称 UHF H₂ 常得到约 1，而纯单重态应为 0；降低能量不等于恢复正确的自旋波函数。

PySCF 的 `spin` 是 $N_\alpha-N_\beta=2S$，不是多重度 $2S+1$。初始密度会决定是否保持或打破空间对称。

## 从公式到程序

1. 固定 STO-3G，对每个 H₂ 坐标同时运行 RHF 和破缺对称初猜的 UHF，保存能量、S²、收敛状态。
2. 对同一 O₂ 几何做 spin=0 和 spin=2 的 UHF；解释哪一个是三重态设置。
3. 固定第一帧 H₂ 几何，依次用控制文件指定的基组做 RHF。输出不同实验到不同 CSV。
4. 将“电子迭代失败”“错误驻点”“方法近似失效”分别举一个例子。

从仓库根目录运行，计算集群上提交 Slurm：

```bash
sbatch scripts/slurm.sh 03_electronic_structure/04_break_hf/run.py --output runs/04_break_hf
```

个人电脑可在已安装环境中用 `python` 替代 `sbatch scripts/slurm.sh`。`--input` 接收输入目录。先看 [output/report.txt](output/report.txt)，再检查 [output/README.md](output/README.md) 所列中间量；JSON 只保留可追溯记录，读懂结果不需要解析它。

## 应当做的实验

- 给拉伸 H₂ 的 UHF 使用对称初猜，观察它是否一直停在 RHF 型解；初猜不是无关紧要的技术细节。
- 在 3 Å 处比较 RHF/UHF 和 S²。解释为什么能量改善与自旋污染可以同时出现。
- 基组增大能改善变分能量，但不会自动加入缺失的多行列式关联。

## 分步提示与资料

[PySCF SCF 用户指南](https://pyscf.org/user/scf.html)。

## 完成后应能解释什么

把 H₂ 拉伸能量与 S² 放在一起读，再分别检查 O₂ 自旋表和基组表。完成后应能指出某个差异属于初猜/驻点、表示空间还是波函数近似，并提出只改变一个因素的后续实验。

## 与前后项目的关系

承接 [手写 RHF](../03_rhf/README.md) 的算法验证；[DFT 与 FCI](../05_dft/README.md) 将提供不同近似和同一有限基组中的多行列式基准。

[输入文件说明](input/README.md) · [输出阅读指南](output/README.md) · [分步提示](hints/README.md) · [全课程实现边界](../../docs/IMPLEMENTATION_BOUNDARIES.md)
