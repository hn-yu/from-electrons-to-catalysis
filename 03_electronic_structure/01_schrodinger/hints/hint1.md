# 提示 1：边界放在什么位置

内部点不包括墙上的两个零值。若你把 n 个点的首尾当墙，却仍用 $h=L/(n+1)$，实际求的是另一个盒子。

```python
h = x[1] - x[0]
wall_left, wall_right = x[0] - h, x[-1] + h
```

由此计算真实 L，再与解析方阱能级比较。有限差分通常低估连续动能；网格加密时应观察到 $O(h^2)$ 误差。
