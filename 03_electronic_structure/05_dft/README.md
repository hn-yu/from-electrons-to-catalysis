# 05 · 同一有限基组里的 HF、DFT 与 FCI

本项目在相同几何和基组下比较 HF、几种密度泛函和 FCI，理解“更低的能量”“更多电子相关”与“更准确的预测”之间的区别。

## 背景：为什么需要这一步

完整多电子波函数随电子数迅速变得复杂。密度泛函理论（DFT）改用三维电子密度作为基本变量；困难转移到了能量如何依赖密度。实际的 Kohn–Sham（KS）方法引入辅助的非相互作用轨道来计算动能，所以运行 DFT 时仍然会看到轨道、占据和 SCF 循环。

经典库仑能可以显式写出，其余交换、关联和动能差异由交换关联泛函近似处理。LDA 使用局域密度信息，GGA 如 PBE 还使用密度梯度，杂化泛函如 B3LYP 混入一部分 HF 交换。它们是不同模型，不能根据最终总能从小到大排列“准确度”。

全组态相互作用（FCI）在选定的有限轨道空间中组合所有允许的行列式，可作为该空间内电子相关的基准。其代价随空间大小呈组合增长，所以这里选很小的 H₂，目的是让方法差异可见，而不是建议给真实金属表面运行 FCI。

## 开始前需要理解的概念

- **密度 n(r)**：积分给出电子总数，不包含完整多电子波函数的全部显式坐标。
- **交换关联泛函 Exc**：KS 分解中需要近似的部分；泛函名称是模型选择，不是收敛精度开关。
- **积分网格**：数值计算密度泛函积分的空间采样；加密网格降低积分误差。
- **RKS**：限制性的 KS 计算，在本项目中使用闭壳层轨道占据。
- **有限基组 FCI**：对当前基组的波函数问题求解；仍有基组误差，也不是实验能量本身。

## 本次任务：从什么得到什么

读取 geometries.csv 中实际列出的 H₂ XYZ，在同一基组下调用 PySCF 的 RHF、RKS 与 FCI。手写方法比较表与相对能量分析：同一几何横向比较方法，固定方法纵向比较键长，再讨论误差随断键怎样变化。目录中的 O₂ 结构不属于这条闭壳层 H₂ 扫描。

## 先用一个小例子走通思路

同一有限基组下，HF 的单行列式属于 FCI 的可变空间，因此 $E_{\rm FCI}\le E_{\rm HF}$。这给出了可以验证的不等式，也定义了该基组内的相关能。

近似 DFT 若低于 FCI，并不说明它突破了同一波函数变分下界：它计算的是另一个近似能量表达式。比较解离行为时，还要看拉伸后的能量变化是否合理，不能只检查平衡点的绝对误差。

## 实现边界

**直接用 PySCF** 的 RKS、积分网格与 FCI；不手写交换关联泛函、PAW 或生产级积分。**手写**统一几何与基组的比较、误差定义和近似解释。FCI 是有限基组基准，不是完备基组实验真值。

## 输入与物理模型

- [H2_0.74.xyz](input/H2_0.74.xyz)
- [H2_2.50.xyz](input/H2_2.50.xyz)
- [O2.xyz](input/O2.xyz)
- [control.toml](input/control.toml)
- [geometries.csv](input/geometries.csv)

每帧 XYZ 分别求 HF、LDA/VWN、PBE、B3LYP 和 FCI。RKS 固定为闭壳层自旋限制形式，基组与坐标相同。

```math
E_{\rm KS}[n]=T_s[n]+\int v_{\rm ext}n\,d\mathbf r+E_H[n]+E_{xc}[n]+E_{NN}.
```

$T_s$ 为非相互作用动能，$E_H$ 是经典电子库仑能；所有遗漏的交换和关联近似进入 $E_{xc}$。不同泛函不满足相互之间的变分排序。

同一基组 Hamiltonian 的全组态相互作用满足

```math
E_{\rm FCI}\le E_{\rm HF},\qquad E_c^{\rm basis}=E_{\rm FCI}-E_{\rm HF}.
```

DFT 近似能量偶尔低于 FCI 并不违反 FCI 对其自身 Hamiltonian 的变分界，因为两者能量泛函不同。

## 从公式到程序

1. 从 geometries.csv 读 XYZ；对所有方法使用同一套输入坐标和 basis。
2. 保存每个 SCF 的收敛状态，DFT 网格 level=3，输出结果 CSV。
3. 逐帧计算 HF−FCI 和 DFT−FCI；另画相对最低点的势能曲线，区分绝对能量偏移与曲线形状。
4. 解释为什么简单提高 DFT 网格密度不能修复限制自旋解离的静态关联问题。

从仓库根目录运行，计算集群上提交 Slurm：

```bash
sbatch scripts/slurm.sh 03_electronic_structure/05_dft/run.py --output runs/05_dft
```

个人电脑可在已安装环境中用 `python` 替代 `sbatch scripts/slurm.sh`。`--input` 接收输入目录。先看 [output/report.txt](output/report.txt)，再检查 [output/README.md](output/README.md) 所列中间量；JSON 只保留可追溯记录，读懂结果不需要解析它。

## 应当做的实验

- 先提高积分网格，再更换泛函，两次实验分别讨论数值误差与模型误差。
- 拉伸时检查曲线形状，不能只根据平衡键长附近一个点给所有方法排名。
- 改用更大基组之前估计 FCI 行列式数：组合数增长解释了为什么真实表面不能照搬这个求解器。

## 分步提示与资料

[PySCF DFT](https://pyscf.org/user/dft.html) 与 [FCI](https://pyscf.org/user/ci.html)。

## 完成后应能解释什么

读每个方法的收敛标记、逐几何能量及相对 FCI 的差值。完成后应能把“提高积分网格”“增加基函数”“更换泛函”各自对应的误差来源写清，并解释为什么小分子上的一次排名不能直接推广到催化表面。

## 与前后项目的关系

前置是 [HF 失效实验](../04_break_hf/README.md)。下一项 [周期固体](../06_periodic/README.md) 保留 DFT 思路，但把孤立分子的基函数和边界条件换成周期表示。

[输入文件说明](input/README.md) · [输出阅读指南](output/README.md) · [分步提示](hints/README.md) · [全课程实现边界](../../docs/IMPLEMENTATION_BOUNDARIES.md)
