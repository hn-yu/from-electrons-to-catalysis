# 01 · 让单位在公式两边闭合

本项目把电子结构、光谱和动力学中常见的单位放到同一个可检查的换算链上，并用热能尺度判断某个能量误差是否会影响结论。

## 背景：为什么需要这一步

电子结构软件常报告 Hartree，表面科学常用 eV，热化学表常用 kJ/mol，振动光谱则用 cm⁻¹。这些数值不能只换标签：有的按单粒子计，有的按一摩尔计，波数还需要通过光子能量关系才转换为能量。

单位检查也是理解模型的入口。“每个原胞总能”与“每个吸附原子的能量”可以同为 eV，却代表不同的归一化；把它们直接比较仍然会出错。程序首先要知道量属于哪一类，再做算术。

催化速率常含势垒与热能之比的指数。因而能量差看起来只有 0.1 eV，不一定是小影响。本项目用 $k_BT$ 作为温度相关的参照，为后续选择收敛阈值提供实际依据。

## 开始前需要理解的概念

- **eV 与 Hartree**：都可以表示单个量子体系的能量；1 Hartree 约为 27.2114 eV。
- **摩尔量**：把每粒子能量乘阿伏伽德罗常数后，才能与每摩尔能量比较。
- **波数**：cm⁻¹ 表示波长的倒数，能量为 hc 乘波数；它不是每秒的振动次数。
- **量纲与归一化**：量纲检查排除 bar→eV，额外的物理说明则排除“每胞”与“每 H”的混用。

## 本次任务：从什么得到什么

逐行读取 conversions.csv，同时用自己的换算链和 ASE 常数换算同一个数。再读取温度表，输出每个温度的 kBT。你要提交换算过程、对照差值，以及对“0.1 eV 势垒误差有多大影响”的量级解释。

## 先用一个小例子走通思路

在 600 K，$k_BT\approx0.05170$ eV。若其他因素保持相同，将一个势垒提高 0.1 eV，速率之比是 $e^{-0.1/0.05170}\approx0.145$，即约慢七倍。

这个算术只使用一个指数比值，不要求你已经掌握完整 TST。它说明误差容限应围绕研究问题选择：允许速率差两倍与允许差十倍，对能量精度的要求不同。

## 实现边界

**手写**量纲换算链和热能、指数敏感性；**直接用库**读取物理常数；**做后比对**自己的换算与 ASE units。不要重写单位数据库，也不要把不同量纲都塞进同一个无标签浮点数。

## 输入与物理模型

- [control.toml](input/control.toml)
- [conversions.csv](input/conversions.csv)
- [temperatures.csv](input/temperatures.csv)

能量可以按每个粒子或每摩尔报告。设 $N_A$ 为阿伏伽德罗常数：

```math
1\ {\rm eV/particle}=eN_A/1000\ {\rm kJ/mol}\simeq96.4853\ {\rm kJ/mol}.
```

振动波数 $\tilde\nu$ 是长度倒数，先乘光速得到频率，再乘普朗克常数：

```math
E=hc\tilde\nu,\qquad k_BT=(8.617333262\times10^{-5}\ {\rm eV/K})T.
```

`conversions.csv` 的三列是数值、源单位、目标单位。`temperatures.csv` 是 K；不要将电子展宽参数当作这个热力学温度。

对同一个前因子，势垒误差 $\delta E$ 导致 $k(E+\delta E)/k(E)=\exp[-\delta E/(k_BT)]$。这是随后选择数值精度的物理依据。

## 从公式到程序

1. 给每种输入单位列出到所属量纲基准的比例；先验证源、目标属于同一量纲。
2. 每条换算同时打印自己的值、ASE 的值和差。常数版本不同可能产生末位差别。
3. 对 300 K 和 600 K 输出 kBT，再算 0.05、0.1 eV 势垒误差导致的速率比。

从仓库根目录运行，计算集群上提交 Slurm：

```bash
sbatch scripts/slurm.sh 02_bringup/01_units/run.py --output runs/01_units
```

个人电脑可在已安装环境中用 `python` 替代 `sbatch scripts/slurm.sh`。`--input` 接收输入目录。先看 [output/report.txt](output/report.txt)，再检查 [output/README.md](output/README.md) 所列中间量；JSON 只保留可追溯记录，读懂结果不需要解析它。

## 应当做的实验

- 把 `1 bar → Pa` 手算成 100000，检查程序。把 bar 转 eV 应当报错；常数表里都有这两个名称不代表允许相除。
- 300 K 的 kBT 约 0.02585 eV，600 K 约 0.05170 eV。预测温度翻倍时，固定势垒误差的影响如何改变。
- 分别解释“每摩尔吸附事件”和“每个表面原胞”对应的归一化，不能只把标签统一成 eV。

## 分步提示与资料

[ASE 单位体系](https://ase-lib.org/ase/units.html)；实际常数版本写入输出记录。

## 完成后应能解释什么

先读 conversions.csv 中 own 与 ASE 两列，区分常数末位差和明显量纲错误；再读 thermal_scales.csv。完成时应能不用程序说出 300/600 K 的 kBT 量级，并从允许的速率倍率反推出能量容限。

## 与前后项目的关系

本项目只需指数、对数与基本单位知识。[能量与力](../02_forces/README.md) 会立即使用 eV/Å；[TST](../../05_kinetics/05_tst/README.md) 则把这里的指数敏感性发展为完整速率公式。

[输入文件说明](input/README.md) · [输出阅读指南](output/README.md) · [分步提示](hints/README.md) · [全课程实现边界](../../docs/IMPLEMENTATION_BOUNDARIES.md)
