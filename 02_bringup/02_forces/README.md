# 02 · 从两原子坐标到一致的能量和力

本项目把 XYZ 中两个原子的坐标转换为距离、能量和两原子的力。我们用可手工求导的 Morse 势，建立以后优化、振动和动力学共同依赖的接口。

## 背景：为什么需要这一步

势能告诉我们哪个构型能量高，力告诉我们从当前构型向哪边移动会降低能量。对一根键，势能随键长变化；对真实多原子体系，能量依赖所有坐标，力是它对每个坐标的负导数。

Morse 势是一个描述键伸缩的简化模型：短距离排斥很强，平衡距离附近近似抛物线，长距离时趋向解离极限。选择它是因为三个区域都有清晰的物理含义，而且能量和导数可精确写出。这里使用 H 元素标签来提供坐标，并不宣称该参数就是高精度的氢分子势能面。

原子坐标有三个分量，而键长是一个标量。径向导数必须沿真实键方向分配给两原子；这是从一维公式连接到原子模拟最容易遗漏的一步。

## 开始前需要理解的概念

- **键长 r**：两核位置差向量的长度，整体平移或旋转不改变它。
- **势阱深度 D**：本例无穷远能量为零，最低点为 −D；统一加能量常数不会改变力。
- **径向力与 Cartesian 力**：前者描述键长方向的收缩/拉伸，后者是每个原子的三分量矢量。
- **解析导数与差分**：前者从公式求导，后者重新计算两次能量；ASE 提供第三条实现路径。

## 本次任务：从什么得到什么

读取 bonds.xyz 的每一帧，计算 r 和单位键向量。手写 Morse 能量及径向力，再转换成两行三分量原子力。对同一构型调用 ASE MorsePotential，并用能量中心差分检查导数。最终把三条路径在同一参数、距离和单位下对齐。

## 先用一个小例子走通思路

在 $r=r_e=0.74$ Å 时，本例能量为 −4.5 eV，力为零。将键拉长，力应把两个原子拉回；将键压短，力应把它们推开。

再把分子从 z 方向旋转到任意方向。能量应该相同，但力的 x/y/z 分量会改变。只在 z 轴上测试，会让“把径向力直接写入 z 分量”的错误隐藏起来。

## 实现边界

**手写后比对** Morse 能量、解析导数和中心差分；用 **ASE MorsePotential** 独立验证。XYZ 读写、原子容器和后续优化器直接用 ASE。

## 输入与物理模型

- [bonds.xyz](input/bonds.xyz)
- [control.toml](input/control.toml)

`bonds.xyz` 是多帧两原子坐标，Å 为长度单位；每帧独立计算距离 $r=|\mathbf R_2-\mathbf R_1|$。

```math
E(r)=D[(1-z)^2-1],\quad z=e^{-a(r-r_e)},\quad F_r=-\frac{dE}{dr}=-2Daz(1-z).
```

$D$ 的单位为 eV，$a$ 为 Å⁻¹，$r_e$ 为 Å。这里无穷远能量为零，平衡点能量为 $-D$。

```math
\mathbf F_2=F_r\frac{\mathbf R_2-\mathbf R_1}{r},\qquad\mathbf F_1=-\mathbf F_2.
```

径向力不是某一个原子的固定 z 分量。ASE 的参数对应关系为 `epsilon=D, r0=re, rho0=a*re`；比较时将截断放到所有测试距离之外。

## 从公式到程序

1. 从每帧 XYZ 计算距离和单位方向，写自己的能量与两原子力。
2. 用中心差分估算同一距离处的径向力。不能把数值梯度和力混为一谈。
3. 给同一 ASE Atoms 挂上 MorsePotential；比较能量、第二个原子力在键方向的投影。
4. 输出 scan.csv 的六列，说明解析值、差分值和库值各来自哪条路径。

从仓库根目录运行，计算集群上提交 Slurm：

```bash
sbatch scripts/slurm.sh 02_bringup/02_forces/run.py --output runs/02_forces
```

个人电脑可在已安装环境中用 `python` 替代 `sbatch scripts/slurm.sh`。`--input` 接收输入目录。先看 [output/report.txt](output/report.txt)，再检查 [output/README.md](output/README.md) 所列中间量；JSON 只保留可追溯记录，读懂结果不需要解析它。

## 应当做的实验

- 在平衡距离两侧选点：短键应排斥，长键应吸引。平衡点 F=0 只能检查一个特殊位置。
- 将整个分子平移并旋转 37°；能量不变，力随坐标旋转。固定 z 方向的错误代码会在这里暴露。
- 给能量统一加常数 10 eV，预测力、振动频率和优化终点是否改变。

## 分步提示与资料

[ASE MorsePotential 源码](https://ase-lib.org/_modules/ase/calculators/morse.html)。

## 完成后应能解释什么

scan.csv 对比能量和径向力；Cartesian-forces.csv 对比每帧两原子的完整力。除了看误差大小，还要解释为什么两原子力之和为零，以及旋转后哪些量不变、哪些量随坐标变化。

## 与前后项目的关系

先掌握 [单位](../01_units/README.md)；接着 [差分失效](../03_finite_differences/README.md) 会研究数值导数为什么不能无限缩小步长。这套接口随后被优化器、MD 和 Hessian 项目复用。

[输入文件说明](input/README.md) · [输出阅读指南](output/README.md) · [分步提示](hints/README.md) · [全课程实现边界](../../docs/IMPLEMENTATION_BOUNDARIES.md)
