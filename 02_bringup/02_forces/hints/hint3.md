# 提示 3：对照必须使用同一势函数

ASE 使用 $\rho_0(r/r_0-1)$ 出现在指数中，因此 $\rho_0=ar_e$。

如果只把 `alpha` 直接传给 `rho0`，两条曲线在平衡点仍可能给出相同能量和零力，但曲率不同。

$$E''(r_e)=2Da^2.$$

另一个差异是截断及其平滑函数。先在远离截断的区域比较，再单独研究截断附近的行为。把截断差异误判成力公式错误，会让后续排错失去方向。
