# 03 · 为什么更小的差分步长反而更差

本项目固定同一个正确的势函数，只改变计算导数的位移步长，观察误差如何先减小再增大。目标是学会选择数值参数，而不是记住一个通用步长。

## 背景：为什么需要这一步

实际电子结构计算未必方便提供每一种高阶导数，因此我们常通过相邻几何的能量或力估算导数。中心差分使用一个点两侧的信息，在光滑函数上比单侧差分更准确。

这种近似有两个竞争的误差来源。步长太大时，局部函数不能被低阶 Taylor 展开准确描述；步长太小时，两个几乎相同的浮点能量相减会丢失有效数字，再除以小步长放大误差。真实 SCF 或采样噪声还会进一步限制可用步长。

我们继续使用 Morse 势，因为它有已知解析力。这样图上的误差可以明确归因于差分本身，而不是把两个未知近似互相当作真值。

## 开始前需要理解的概念

- **中心差分**：用 E(r+h) 和 E(r−h) 近似局部斜率，力还要加负号。
- **截断误差**：Taylor 展开略去高阶项造成的误差，在合适范围内约按 h² 变化。
- **舍入与消减**：浮点数精度有限，相近大数相减后的小差可能不精确。
- **绝对/相对误差**：真实力接近零时，相对误差分母也接近零，应先看绝对误差。

## 本次任务：从什么得到什么

从 bonds.xyz 读取实际距离，用解析式得到基准力。按 control.toml 扫描 50 个对数间隔步长，保存每次差分力误差，画 log–log 曲线。选几个位于误差谷底两侧的点，分别解释主导误差来源。

## 先用一个小例子走通思路

若某一段确实由二阶截断误差主导，把 h 减半应使误差约变为四分之一。继续缩小后这个规律失效，说明你已经进入舍入或噪声主导区。

给能量统一加很大的常数时，真实力仍然相同，但差分可能更差：计算机在两个更大的数之间寻找同样小的能量差。这个实验把“物理零点不影响力”与“数值表示影响差分”分开了。

## 实现边界

**手写**中心差分和误差扫描；解析 Morse 力作为独立公式基准。NumPy 提供数组和浮点运算，画图可直接用 Matplotlib；不需要调用另一套差分包装器来掩盖误差来源。

## 输入与物理模型

- [bonds.xyz](input/bonds.xyz)
- [control.toml](input/control.toml)

对 XYZ 给出的距离 $r$，中心差分为

```math
F_h=-\frac{E(r+h)-E(r-h)}{2h}=F(r)-\frac{E′′′(r)}{6}h^2+O(h^4).
```

上式三阶导数项解释了理想的二阶收敛。浮点相减则引入约 $\epsilon_{\rm mach}|E|/h$ 的误差，因此总误差的模型为

```math
\varepsilon(h)\approx Ah^2+B\epsilon_{\rm mach}/h.
```

`control.toml` 指定对数步长范围与采样点数。几何距离由 `bonds.xyz` 读取，不能继续使用硬编码位置。

## 从公式到程序

1. 对每个 h 重新求两侧能量，用解析力算绝对误差。
2. 在 log–log 坐标画误差对 h；在大 h 区估计斜率是否接近 2。
3. 查最优 h 所在量级，再检查极小 h 时两个能量是否已被舍入成同一个数。
4. 保留每一个采样点，不要只输出“最佳步长”。

从仓库根目录运行，计算集群上提交 Slurm：

```bash
sbatch scripts/slurm.sh 02_bringup/03_finite_differences/run.py --output runs/03_finite_differences
```

个人电脑可在已安装环境中用 `python` 替代 `sbatch scripts/slurm.sh`。`--input` 接收输入目录。先看 [output/report.txt](output/report.txt)，再检查 [output/README.md](output/README.md) 所列中间量；JSON 只保留可追溯记录，读懂结果不需要解析它。

## 应当做的实验

- 在平衡点与非平衡点各扫描一次；近零导数的相对误差会失去意义，改看绝对误差。
- 对能量加一个很大的常数再差分：解析力不变，差分舍入误差却可能变大。
- 模拟电子结构能量噪声，并预测最优步长向哪边移动；梯度验证必须考虑 SCF 精度。

## 分步提示与资料

本项目的基准公式来自上一项目 Morse 势；浮点工具见 [NumPy spacing](https://numpy.org/doc/stable/reference/generated/numpy.spacing.html)。

## 完成后应能解释什么

读取 scan.csv 并找出误差最小的区间，而不只抄出一个 best_step。完成时要能说明为什么小步长不总更好，以及若能量本身含 SCF 噪声，应该如何联合收紧电子精度和选择位移。

## 与前后项目的关系

这是 [能量与力](../02_forces/README.md) 的精度实验；[Hessian](../../05_kinetics/03_hessian/README.md) 会把同样的问题应用到力的差分和振动频率。

[输入文件说明](input/README.md) · [输出阅读指南](output/README.md) · [分步提示](hints/README.md) · [全课程实现边界](../../docs/IMPLEMENTATION_BOUNDARIES.md)
